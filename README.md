# 💬 Chat Ziro

실시간 채팅 앱 + 텔레그램 알림

## ✨ 기능

- 🔗 **링크 공유**: 링크만 있으면 누구나 채팅 참여
- 👤 **프로필 설정**: 닉네임 + 프로필 이미지
- 💬 **실시간 채팅**: Socket.io 기반 실시간 통신
- 📁 **여러 채팅방**: 탭으로 주제별 채팅방 관리
- ✏️ **메시지 수정**: 보낸 메시지 수정 가능
- 💾 **백업**: JSON 형식으로 채팅 내용 다운로드
- 📱 **텔레그램 알림**: 상대방 메시지 알림 (본인 제외)

---

## 🚀 시작하기

### 1. 필수 요구사항

- **Node.js 18 이상** 설치
  - [https://nodejs.org](https://nodejs.org)에서 다운로드

### 2. 설치

```bash
# 패키지 설치
npm install
```

### 3. 환경 설정

```bash
# .env 파일 생성
copy .env.example .env
```

`.env` 파일을 열고 텔레그램 봇 토큰을 설정하세요 (선택사항).

### 4. 실행

```bash
npm start
```

브라우저에서 `http://localhost:3000` 접속!

---

## 📱 텔레그램 알림 설정

### 봇 토큰 얻기 (서버 관리자)

1. 텔레그램에서 **@BotFather** 검색
2. `/newbot` 명령어 입력
3. 봇 이름과 username 입력
4. 받은 토큰을 `.env` 파일의 `TELEGRAM_BOT_TOKEN`에 입력

### Chat ID 얻기 (사용자)

1. 텔레그램에서 **@userinfobot** 검색
2. `/start` 또는 아무 메시지 전송
3. 받은 Chat ID를 채팅 앱 프로필 설정에 입력

---

## ☁️ 무료 배포 (Render.com)

### 1. GitHub에 코드 업로드

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/your-username/chat-ziro.git
git push -u origin main
```

### 2. Render.com에서 배포

1. [render.com](https://render.com) 가입
2. "New +" → "Web Service" 클릭
3. GitHub 연결 후 저장소 선택
4. 설정:
   - **Name**: chat-ziro (원하는 이름)
   - **Runtime**: Node
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
5. Environment Variables에서 `TELEGRAM_BOT_TOKEN` 추가 (선택)
6. "Create Web Service" 클릭

배포 완료 후 `https://your-app.onrender.com` 주소로 접속!

> ⚠️ **무료 플랜 주의사항**: 15분간 비활성 시 슬립 모드로 전환됩니다.
> 다시 접속하면 30초~1분 후 활성화됩니다.

---

## 📂 프로젝트 구조

```
chat_ziro/
├── public/
│   ├── index.html      # 메인 UI
│   ├── style.css       # 스타일
│   └── app.js          # 클라이언트 코드
├── uploads/            # 프로필 이미지 저장
├── server.js           # Express + Socket.io 서버
├── database.js         # SQLite 데이터베이스
├── telegram.js         # 텔레그램 알림
├── chat.db             # 데이터 파일 (자동 생성)
├── package.json
├── .env                # 환경 변수 (직접 생성)
├── .env.example        # 환경 변수 예시
└── README.md
```

---

## 🔧 개발

```bash
# 개발 모드 실행
npm run dev
```

---

## 📝 백업 파일 형식 (JSON)

```json
{
  "roomName": "일반",
  "createdAt": "2025-12-23T10:30:00.000Z",
  "archivedAt": null,
  "participants": [
    {
      "nickname": "사용자1",
      "profileImage": "/uploads/abc123.jpg"
    }
  ],
  "messages": [
    {
      "id": "msg-uuid",
      "sender": "사용자1",
      "senderProfileImage": "/uploads/abc123.jpg",
      "text": "안녕하세요!",
      "time": "2025-12-23T10:30:15.000Z",
      "isEdited": false
    }
  ]
}
```

나중에 이 JSON 파일로 채팅 뷰어 웹페이지를 만들 수 있습니다!

---

## 🤝 라이선스

MIT License



